{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_allowedHostsComment": "Restricted to loopback values per the MCP C# SDK's transport guidance (DNS-rebinding protection) — see README 'Security notes'.",
  "AllowedHosts": "localhost;127.0.0.1;[::1]",
  "DemoMode": {
    "_comment": "Flip this to true to swap the well-described get_rates_and_availability tool for its deliberately vague twin, with no code changes. See README.md, 'Demo script — part 2'.",
    "UseVagueToolDescriptions": false
  }
}